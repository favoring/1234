long comp(int n)
{
    long ans=1;
    if(a[n]>0)
    return a[n];
    for(int i=1;i<=n/2;i++)
    ans+=comp(i);
    a[n]=ans;
}
int main()
{
 while(cin>>n){
   memset(a,sizeof(a),0);
   a[1]=1;
   cout<<comp(n)<<endl;
 }
return 0;
}
